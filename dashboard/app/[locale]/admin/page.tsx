import { DashboardShell } from '../../../components/dashboard-shell';
export default async function Admin({params}:{params:Promise<{locale:string}>}){const {locale}=await params,ar=locale==='ar';return <DashboardShell locale={locale} role="admin"><h1>{ar?'لوحة إدارة تريبي':'Tripi Admin'}</h1><p className="muted">{ar?'إدارة المنصة ومراقبة النشاط':'Platform operations and moderation'}</p><section className="grid">{[[ar?'المنظمون النشطون':'Active organizers','18'],[ar?'الرحلات المنشورة':'Published trips','42'],[ar?'الحجوزات المؤكدة':'Confirmed bookings','611'],[ar?'الشكاوى المفتوحة':'Open complaints','3']].map(x=><article className="card" key={x[0]}><span className="muted">{x[0]}</span><div className="metric">{x[1]}</div></article>)}</section></DashboardShell>}
