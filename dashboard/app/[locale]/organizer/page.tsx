import { DashboardOverview } from '../../../components/dashboard-overview';
import { DashboardShell } from '../../../components/dashboard-shell';
export default async function Organizer({params}:{params:Promise<{locale:string}>}){const {locale}=await params,ar=locale==='ar';return <DashboardShell locale={locale} role="organizer"><header className="page-header"><div><h1>{ar?'مرحبًا بك في تريبي':'Welcome to Tripi'}</h1><p className="muted">{ar?'إدارة رحلاتك وحجوزاتك':'Manage your trips and bookings'}</p></div><span className="badge">{ar?'منظم':'Organizer'}</span></header><DashboardOverview locale={locale} role="organizer"/></DashboardShell>}
