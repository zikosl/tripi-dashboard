import Link from 'next/link';
export function DashboardShell({ locale, role, children }: { locale:string; role:'admin'|'organizer'; children:React.ReactNode }) { const ar=locale==='ar'; const links=role==='admin'?[['organizers',ar?'المنظمون':'Organizers'],['trips',ar?'الرحلات':'Trips'],['bookings',ar?'الحجوزات':'Bookings'],['payments',ar?'المدفوعات':'Payments'],['audit-logs',ar?'سجل التدقيق':'Audit logs']]:[['trips',ar?'الرحلات':'Trips'],['bookings',ar?'الحجوزات':'Bookings'],['payments',ar?'المدفوعات':'Payments'],['team',ar?'الفريق':'Team'],['analytics',ar?'التحليلات':'Analytics']]; return <div className="shell"><aside className="sidebar"><div className="brand">Tripi</div><div className="tagline">{ar?'لوحة تحكم تريبي':'Tripi Dashboard'}</div><nav className="nav"><Link href={`/${locale}/${role}`}>{ar?'نظرة عامة':'Overview'}</Link>{links.map(([path,label])=><Link key={path} href={`/${locale}/${role}/${path}`}>{label}</Link>)}</nav></aside><main className="main">{children}</main></div>; }
